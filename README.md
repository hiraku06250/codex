# Management App

This is a minimal React application for a small confectionery business in Okinawa. The app allows management of customers, orders, purchases and monthly summaries. It is implemented in a single HTML file with React and Tailwind via CDN.

Open `index.html` in a browser to start the application. Data is kept only in memory for the session.
